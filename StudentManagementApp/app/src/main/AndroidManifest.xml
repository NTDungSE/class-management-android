<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StudentManagementApp"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.activities.auth.LoginActivity" />
        <activity android:name=".view.activities.auth.RegisterActivity" />
        <activity android:name=".view.activities.students.StudentListActivity" />
        <activity android:name=".view.activities.students.AddStudentActivity" />
        <activity
            android:name=".view.activities.teacher.TeacherDashboardActivity"
            android:exported="false" />
        <activity
            android:name=".view.activities.student.StudentDashboardActivity"
            android:exported="false" />
        <activity android:name=".view.activities.teacher.classes.AddClassActivity" />
        <activity android:name=".view.activities.teacher.classes.EditClassActivity" />
        <activity android:name=".view.activities.teacher.classes.ClassListActivity" />
        <activity android:name=".view.activities.teacher.classes.AssignStudentsActivity" />
    </application>
</manifest>